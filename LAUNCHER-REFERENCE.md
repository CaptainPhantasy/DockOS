# VSD M18 Stream Dock -- Complete Launcher Reference

208 items across 14 pages in 5 scenes. Each item has a `.command` launcher stub in `~/Desktop/VSD-Replacement/v2/launchers/`. The `dev` kind items route through `vsd-launch` (Python dispatcher) which auto-detects the project runner (npm/node